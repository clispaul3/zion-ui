{
  "type": "[[Table]]",
  "props": {
    "controlKey": "{{Service.controlKey.Table}}",
    "rowKey": "parameter",
    "columns": [
      {
        "title": "参数",
        "dataIndex": "parameter"
      },
      {
        "title": "说明",
        "dataIndex": "instructions",
        "render": "{{Service.renderInstructions}}"
      },
      {
        "title": "类型",
        "dataIndex": "type",
        "render": "{{Service.renderType}}"
      },
      {
        "title": "可选择",
        "dataIndex": "select",
        "render": "{{Service.renderInstructions}}"
      },
      {
        "title": "是否必填",
        "dataIndex": "isNeed"
      }
    ],
    "scroll": "{{Service.render_scroll}}",
    "bordered": false,
    "httpConfig": {
      "init": "{{Service.table_httpConfig.init}}"
    }
  }
}