/**
 * @description 表单元素设置
 */
import { FormDesigner } from "../../../form-designer"

export const FormItemConfig = ({ controlKey, config, onOk }) => {
  FormDesigner({
    config,
    controlKey: "",
    onOk: () => {

    }
  })
}